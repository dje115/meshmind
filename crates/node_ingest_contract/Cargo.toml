[package]
name = "node_ingest_contract"
version.workspace = true
edition.workspace = true
description = "Normalized ingestion contract between ingestion agents and MeshMind core"

[dependencies]
serde = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }
